name: Release Please - Simple Example

on:
  push:
    branches:
      - main
    paths:
      - 'simple_example/**'

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          path: simple_example
          release-type: simple
          token: ${{ secrets.GITHUB_TOKEN }}
